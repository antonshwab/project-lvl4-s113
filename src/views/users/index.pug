extends /layouts/application.pug

block content
  table.table.table-stripped
    thead
      tr
        th Full Name
        th Email
        th Created At
        th User id
    tbody
      each user in users
        tr
          td= user.fullName
          td= user.email
          td= user.createdAt
          td= user.id
